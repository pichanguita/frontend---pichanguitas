/**
 * Claves de almacenamiento en localStorage.
 *
 * Fuente única: el nombre del store persistido de Zustand y cualquier lectura
 * directa de localStorage deben referenciar estas constantes (no strings sueltos).
 */
export const AUTH_STORAGE_KEY = 'admin-auth-storage'
